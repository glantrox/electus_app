import 'package:flutter/material.dart';

class AppColors {
  // Surface Colors
  static const surface = Color(0xFF10141A);
  static const surfaceDim = Color(0xFF10141A);
  static const surfaceBright = Color(0xFF353940);
  static const surfaceContainerLowest = Color(0xFF0A0E14);
  static const surfaceContainerLow = Color(0xFF181C22);
  static const surfaceContainer = Color(0xFF1C2026);
  static const surfaceContainerHigh = Color(0xFF262A31);
  static const surfaceContainerHighest = Color(0xFF31353C);

  // On-Surface & Inverse
  static const onSurface = Color(0xFFDFE2EB);
  static const onSurfaceVariant = Color(0xFFB9CACB);
  static const inverseSurface = Color(0xFFDFE2EB);
  static const inverseOnSurface = Color(0xFF2D3137);

  // Outline
  static const outline = Color(0xFF849495);
  static const outlineVariant = Color(0xFF3A494B);
  static const surfaceTint = Color(0xFF00DBE7);

  // Primary (Neon Accents)
  static const primary = Color(0xFFE1FDFF);
  static const onPrimary = Color(0xFF00363A);
  static const primaryContainer = Color(0xFF00F2FF);
  static const onPrimaryContainer = Color(0xFF006A71);
  static const inversePrimary = Color(0xFF00696F);

  // Secondary (Lime Green)
  static const secondary = Color(0xFFFFFFFF);
  static const onSecondary = Color(0xFF1F3700);
  static const secondaryContainer = Color(0xFFA0FB00);
  static const onSecondaryContainer = Color(0xFF457000);

  // Tertiary (Purple/AI features)
  static const tertiary = Color(0xFFFEF5FF);
  static const onTertiary = Color(0xFF480081);
  static const tertiaryContainer = Color(0xFFEAD2FF);
  static const onTertiaryContainer = Color(0xFF8624DE);

  // Error
  static const error = Color(0xFFFFB4AB);
  static const onError = Color(0xFF690005);
  static const errorContainer = Color(0xFF93000A);
  static const onErrorContainer = Color(0xFFFFDAD6);

  // Fixed Variants
  static const primaryFixed = Color(0xFF74F5FF);
  static const primaryFixedDim = Color(0xFF00DBE7);
  static const onPrimaryFixed = Color(0xFF002022);
  static const onPrimaryFixedVariant = Color(0xFF004F54);

  // Background
  static const background = Color(0xFF10141A);
  static const onBackground = Color(0xFFDFE2EB);
  static const surfaceVariant = Color(0xFF31353C);

  // AI/Glassmorphism Specific (Manual entries from description)
  static const coreBlack = Color(0xFF000000);
  static const elevatedSurface = Color(0xFF0D1117);
  static const glassBackground = Color(0xB30D1117); // 0.7 opacity
}
